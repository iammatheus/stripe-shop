import { styled } from "@stitches/react";

export const SelectQuantityContainer = styled("select", {
  width: "40px",
  height: "25px",
  borderRadius: 4,
  cursor: "pointer",
  background: "#202024",
});
